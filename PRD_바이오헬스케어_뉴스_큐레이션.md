# PRD: 바이오/헬스케어 뉴스 큐레이션 웹사이트

- 문서 버전: v1.0
- 작성일: 2026-07-30
- 작성자: 현아 (with Claude)
- 상태: Draft

---

## 1. 개요

미국 바이오/헬스케어 업계의 최신 연구·사업 뉴스를 자체 스크래핑으로 수집하고, 카테고리별로 분류·요약하여 보여주는 웹사이트를 만든다. 뉴스 API나 유료 스크래핑 서비스(ScraperAPI, Diffbot, NewsAPI 등)를 쓰지 않고, 대상 사이트를 직접 크롤링/파싱하는 자체 파이프라인으로 데이터를 확보한다.

### 배경 / 문제 정의

바이오/헬스케어 업계 종사자(특히 마케팅, 신사업, 서비스 기획 담당자)는 미국 시장의 최신 동향을 파악하기 위해 여러 매체를 매일 돌아다니며 확인해야 한다. 정보가 분산되어 있고, 어떤 뉴스가 "연구"인지 "사업/마케팅"인지 구분 없이 섞여 있어 원하는 정보만 빠르게 훑기 어렵다.

### 목적

1. 미국 바이오/헬스케어 관련 뉴스를 매일 자동 수집한다.
2. 마케팅, 신사업, 서비스 관련 뉴스를 우선적으로 노출한다.
3. 뉴스를 카테고리로 분류하고, 각 뉴스에 짧은 요약을 붙여 빠르게 훑어볼 수 있게 한다.

---

## 2. 목표 및 성공 지표

| 목표 | 지표 |
|---|---|
| 매일 신선한 뉴스 확보 | 하루 1회 이상 수집 배치 실행, 소스당 수집 성공률 95% 이상 |
| 카테고리 분류 정확도 | 샘플 100건 수동 검수 시 분류 정확도 85% 이상 |
| 요약 품질 | 원문 대비 핵심 정보(주체/행위/의의) 누락 없이 2~3문장 요약 |
| 중복 제거 | 동일 사건 다중 소스 보도 시 중복률 5% 이하로 병합 |
| 사이트 체감 속도 | 목록 페이지 로딩 1초 이내(캐시 기준) |

---

## 3. 타겟 사용자

- 국내 제약/바이오/헬스케어 기업의 마케팅, 신사업, 사업개발(BD) 담당자
- 미국 시장 동향을 참고해야 하는 전략기획/투자 담당자
- 1차 사용자: 현아 (개인 정보 수집 및 업무 참고용)

---

## 4. 범위

### In Scope (MVP)

- 미국 바이오/헬스케어 뉴스 자동 수집 (RSS 우선, RSS 미제공 시 HTML 파싱)
- 뉴스 카테고리 자동 분류
- 뉴스별 요약 자동 생성
- 카테고리/키워드/날짜 필터가 있는 뉴스 목록 웹페이지
- 원문 링크 제공 (본문 전체 재게시 금지, 저작권 리스크 최소화)

### Out of Scope (추후 검토)

- 회원가입/로그인, 개인화 추천
- 이메일 뉴스레터 발송
- 한국어 번역
- 유료 구독/결제
- 미국 외 지역(유럽, 아시아 등) 뉴스

---

## 5. 뉴스 소스 (MVP 대상)

우선순위 1군: 바이오/헬스케어 전문 매체

| 매체 | 유형 | 수집 방식(우선순위) |
|---|---|---|
| Fierce Biotech | 업계 전문지 | RSS (자체 RSS 피드 제공 확인됨) |
| Fierce Pharma | 업계 전문지 | RSS |
| Endpoints News | 업계 전문지 (바이오텍 딜/투자 특화) | RSS 확인 후 없으면 HTML 파싱 |
| STAT News | 헬스케어 저널리즘 | RSS 확인 후 없으면 HTML 파싱 |
| MedCity News | 헬스케어 비즈니스/스타트업 | RSS |

우선순위 2군: 기업 뉴스룸/보도자료 (마케팅·신사업·서비스 뉴스 확보용)

- 주요 제약사(Pfizer, J&J, Merck, Eli Lilly, AbbVie 등) 공식 Newsroom/Press Release 페이지
- 주요 헬스케어 서비스 기업(UnitedHealth, CVS Health 등) 뉴스룸
- 대다수 기업 뉴스룸은 RSS를 제공하므로 우선 확인 후, 없으면 목록 페이지 HTML 파싱

우선순위 3군 (백로그, MVP 이후 검토): NIH, PubMed 등 연구기관 발표 자료 — "연구" 카테고리 보강용

> 참고: 소스별로 RSS 제공 여부와 robots.txt 정책이 다르므로, 실제 구현 착수 시 소스 목록마다 (1) RSS 유무, (2) robots.txt 크롤링 허용 범위, (3) 페이지 구조를 먼저 점검하는 "소스 온보딩 체크리스트"를 운영한다.

---

## 6. 핵심 기능 요구사항

### 6.1 수집 (자체 스크래핑)

- FR-1: 등록된 소스 목록을 주기적으로(기본 1일 1~4회) 순회하며 신규 기사를 수집한다.
- FR-2: RSS 피드가 있는 소스는 RSS를 우선 사용하고, 없는 소스는 목록 페이지를 HTML 파싱한다.
- FR-3: 각 기사에서 제목, 원문 URL, 발행일, 매체명, 본문(요약 생성용 원문 텍스트)을 추출한다.
- FR-4: 동일 URL 또는 제목 유사도가 높은 기사는 중복으로 처리하여 1건으로 병합한다.
- FR-5: 수집 실패(구조 변경, 차단 등) 시 소스별 실패 로그를 남기고 알림한다.
- FR-6: 수집 대상 사이트의 robots.txt를 확인하여 크롤링이 금지된 경로는 수집하지 않는다.

### 6.2 카테고리 분류

- FR-7: 수집된 기사를 아래 카테고리 체계(7절)에 따라 자동 분류한다.
- FR-8: 한 기사가 여러 카테고리에 해당할 수 있으며, 이 경우 주 카테고리 1개 + 부 카테고리(복수)를 부여한다.
- FR-9: "마케팅", "신사업", "서비스" 카테고리는 목록 화면 상단/필터에서 우선 노출한다.

### 6.3 요약

- FR-10: 각 기사에 대해 2~3문장 요약을 자동 생성한다 (주체 - 행위 - 의의/숫자 구조 권장).
- FR-11: 요약은 원문 문장을 그대로 복사하지 않고 재구성하여 저작권 리스크를 낮춘다.
- FR-12: 요약 생성이 실패하면 원문 첫 문단을 대체 표시하고 "요약 생성 실패" 플래그를 남긴다.

### 6.4 웹 화면

- FR-13: 뉴스 목록 화면은 카드 형태로 제목, 매체, 발행일, 카테고리 태그, 요약을 보여준다.
- FR-14: 카테고리, 매체, 날짜 범위로 필터링할 수 있다.
- FR-15: 키워드 검색을 지원한다.
- FR-16: 각 카드는 원문으로 이동하는 링크를 제공한다 (본문 전체 재게시 없음, 명확한 출처 표기).

---

## 7. 카테고리 체계 (제안)

요청하신 대로 마케팅/신사업/서비스를 중심에 두고, 연구/투자 카테고리로 맥락을 보완하는 구조입니다.

| 카테고리 | 설명 | 예시 |
|---|---|---|
| 마케팅/브랜딩 | 캠페인, 브랜드 전략, DTC(소비자 직접) 마케팅, 파트너십 홍보 | 신약 런칭 캠페인, 인플루언서 협업 |
| 신사업/전략 | 신규 사업 영역 진출, 조직 개편, 전략적 제휴/파트너십 | 디지털 헬스 사업부 신설, 이종산업 제휴 |
| 서비스 출시/업데이트 | 신규 서비스/플랫폼 출시, 앱/디지털 헬스 서비스 업데이트 | 원격진료 서비스 출시, AI 진단 툴 공개 |
| R&D/임상 연구 | 신약 개발, 임상시험 결과, 연구 성과 | 임상 3상 결과 발표 |
| 투자/M&A | 인수합병, 투자 유치, IPO | 시리즈 B 투자 유치 |
| 규제/정책 | FDA 승인, 정책 변화 | FDA 승인 소식 |
| 기타 | 위 카테고리에 속하지 않는 뉴스 | - |

분류 로직(1차 규칙 기반 + 필요시 보강)은 8.3절 참고.

---

## 8. 기술 아키텍처

### 8.1 전체 구성

```
[스케줄러] --트리거--> [수집기(Collector)] --원문--> [파서/정제(Parser)]
                                                        |
                                                        v
                                              [중복제거(Dedup)]
                                                        |
                                                        v
                                        [분류(Classifier)] --> [요약(Summarizer)]
                                                        |
                                                        v
                                                  [DB 저장]
                                                        |
                                                        v
                                      [API 서버] <---- [웹 프론트엔드]
```

### 8.2 수집기 (Collector) — 자체 스크래핑

- 언어/도구: Python (requests/httpx + BeautifulSoup, 필요시 Playwright로 JS 렌더링 페이지 대응)
- 방식:
  1. RSS 피드가 있는 소스는 `feedparser`로 파싱 (가장 안정적, 우선 적용).
  2. RSS가 없는 소스는 목록 페이지 HTML을 파싱해 기사 링크를 추출하고, 개별 기사 페이지를 재요청해 본문을 추출.
  3. 소스별로 파서를 분리 구현 (사이트 구조가 다르므로 소스마다 CSS 셀렉터/파싱 규칙을 별도 관리).
- 준수 사항: robots.txt 확인, 소스별 요청 간 지연(rate limit, 예 1~2초), User-Agent에 연락처/용도 명시, 사이트 부하를 주지 않는 수준으로 요청.
- 실행 주기: 스케줄러(cron 또는 Airflow/APScheduler)로 1일 1~4회 배치 실행.
- 장애 대응: 소스별 파서가 사이트 구조 변경으로 깨질 수 있으므로, 수집 성공률 모니터링 및 실패 알림 체계 필요.

### 8.3 분류 (Classifier)

- 1차: 키워드/규칙 기반 분류 (제목/본문에 "launch", "partnership", "campaign", "clinical trial" 등 카테고리별 키워드 사전 매칭).
- 2차(정확도 보강, 선택): 규칙 기반으로 애매한 건에 한해 경량 텍스트 분류 모델(오픈소스 임베딩 + 분류기, 또는 LLM 프롬프트 분류) 적용.
- 분류 결과는 신뢰도 점수와 함께 저장해, 추후 수동 보정 데이터로 재학습에 활용 가능하도록 설계.

### 8.4 요약 (Summarizer)

> 오픈 이슈: "유료 API 비용을 쓰지 않는다"는 원칙이 스크래핑(데이터 수집)에 대한 것인지, 요약 생성까지 포함하는지 확인 필요. 요약은 별도 비용 요소이므로 아래 옵션 중 선택이 필요함.

| 옵션 | 비용 | 품질 | 비고 |
|---|---|---|---|
| A. 추출 요약(Extractive, 규칙/통계 기반) | 무료 | 중~하 | 본문에서 핵심 문장 그대로 추출, 구현 간단 |
| B. 자체 호스팅 오픈소스 LLM (예: 경량 요약 모델) | 인프라 비용만 | 중~상 | 서버/GPU 비용 발생 가능 |
| C. LLM API (Claude 등) 활용 | 사용량 기반 과금 | 상 | 소량 뉴스 요약은 비용 부담이 크지 않은 편 |

MVP 권장안: 우선 A(추출 요약)로 비용 없이 시작하고, 품질이 부족하면 소량 예산으로 C를 병행 검토.

### 8.5 데이터 모델 (예시)

```
Article
- id
- source_name        (매체명)
- source_url          (원문 링크)
- title
- published_at
- collected_at
- raw_text            (원문 본문, 요약 생성용)
- summary             (생성된 요약)
- primary_category
- secondary_categories[]
- dedup_group_id
- classification_confidence
```

### 8.6 저장/서빙

- DB: PostgreSQL (또는 SQLite로 MVP 시작 후 전환)
- API: 목록/필터/검색 API (REST)
- 프론트엔드: 카드형 목록 UI (React 또는 정적 사이트 생성 방식 모두 가능, MVP는 단순 서버 렌더링도 무방)
- 배포: 소규모 트래픽 기준 저비용 호스팅(VM 1대 + 스케줄러)으로 충분

---

## 9. 법적/윤리적 고려사항

- 공개된 데이터를 스크래핑하는 것 자체는 미국에서 hiQ v. LinkedIn 판례 이후 CFAA 위반으로 보지 않는 경향이나, robots.txt와 각 사이트 이용약관(ToS)은 계속 준수해야 한다. robots.txt는 법적 강제력은 없지만 무시할 경우 분쟁 시 불리한 정황 증거가 될 수 있다.
- 저작권: 기사 본문 전체를 복제/재게시하지 않는다. 요약은 원문을 그대로 옮기지 않고 재구성하며, 항상 원문 링크와 출처(매체명)를 명시한다 (Fair use 원칙에 부합하도록 설계).
- 로그인/페이월이 있는 콘텐츠는 수집하지 않는다 (인증 우회는 법적 리스크가 크므로 MVP 범위에서 제외).
- 개인정보(PII)는 수집 대상에 포함하지 않는다.
- 소스별 이용약관에 스크래핑 명시적 금지 조항이 있는지 온보딩 체크리스트에서 확인한다.

---

## 10. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 대상 사이트 구조 변경으로 파서 고장 | 수집 누락 | 소스별 파서 모니터링 + 실패 알림, 빠른 핫픽스 프로세스 |
| 사이트 측 크롤링 차단(IP 차단 등) | 수집 중단 | 요청 빈도 조절, robots.txt 준수, 필요시 소스 교체 |
| 저작권/ToS 분쟁 | 서비스 중단 리스크 | 본문 비게시, 출처 명시, 소스별 약관 사전 검토 |
| 분류/요약 품질 저하 | 사용자 신뢰 하락 | 정기 샘플 검수, 피드백 반영 루프 구축 |
| 개인용에서 확장 시 트래픽 증가 | 인프라 비용 증가 | 캐싱, 정적 페이지화 검토 |

---

## 11. 로드맵 (제안)

- MVP (2~3주): 소스 3~5개 RSS 기반 수집 + 규칙 기반 분류 + 추출 요약 + 기본 목록/필터 UI
- v1.1: HTML 파싱 소스 확대(기업 뉴스룸), 중복 제거 고도화, 검색 기능
- v1.2: 분류/요약 품질 개선(LLM 보강 검토), 카테고리 체계 사용자 피드백 반영
- v2: NIH/PubMed 등 연구기관 소스 추가, 대시보드/트렌드 시각화 검토

---

## 12. 오픈 이슈 (결정 필요)

1. 요약 생성 방식: 무비용 추출 요약으로 시작할지, 소량 LLM API 비용을 허용할지.
2. 개인 사용 범위를 넘어 외부 공개 시, 소스별 ToS 재검토 필요 (특히 STAT News, Endpoints News는 유료 콘텐츠 비중이 있어 확인 필요).
3. RSS 미제공 소스(예: 일부 기업 뉴스룸)의 HTML 구조 변경 대응 리소스를 누가/어떻게 유지보수할지.
